import { DateSeparatorPipe } from './date-separator.pipe';

describe('DateSeporatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
