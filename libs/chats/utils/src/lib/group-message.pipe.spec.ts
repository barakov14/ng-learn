import { GroupMessagePipe } from './group-message.pipe';

describe('GroupMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
