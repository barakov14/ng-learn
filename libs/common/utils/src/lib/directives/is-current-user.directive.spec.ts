import { IsCurrentUserDirective } from './is-current-user.directive';

describe('IsCurrentUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IsCurrentUserDirective();
    expect(directive).toBeTruthy();
  });
});
